import React from 'react';
import Body from '../components/body.tsx';

function Projects() {
  const title = "Projects";
  const body = "Here are my projects...";

  return (
    < Body title={title} body={body} />
  )
}

export default Projects;
